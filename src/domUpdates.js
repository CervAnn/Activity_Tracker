$import $ from 'jquery';
import userDataSample1 from '../data/userDataSample1';

export default = {
  displayStepGoal() {
    //grab userData.stepgoals and reduce to average
    //subtract from user[0] step goal
    //if difference < 0, add... or something
    //$firstSpan.innertext(difference)
  }
}